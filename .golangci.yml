version: "2"
run:
  build-tags:
    - acceptance
formatters:
  enable:
    - gofumpt
    - gci
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/screwyprof/)
      custom-order: true
    gofumpt:
      module-path: github.com/screwyprof/delegator
